services:
  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6333:6333"
    volumes:
      - qdrant_data:/qdrant/storage

  backend:
    build: 
      context: ../backend
    environment:
      - ENV=local
      - QDRANT_URL=http://qdrant:6333
      - SEC_USER_AGENT=VaishnaviKashyap SECFilingsCopilot vaishnavikashyap@gmail.com
      - DATA_DIR=/app/data
    ports:
      - "8000:8000"
    volumes:
      - ../backend/data:/app/data
    depends_on:
      - qdrant

  frontend:
    build: 
      context: ../frontend
    environment:
      - NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
    ports:
      - "3000:3000"
    depends_on:
      - backend

volumes:
  qdrant_data: